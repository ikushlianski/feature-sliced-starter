import React from "react";

export const CreateProjectPage = () => {
  return (
    <div>
      <p>Create project here</p>
      <p>Uses components from `widgets`, `features` and/or `entities` layers</p>
    </div>
  );
};
